#!/usr/bin/env sol-fbp-runner

# Reads a int (sol_irange) from socket and prints to console.
# See int-writer.fbp, that should be executed before this.

socket_int(unix-socket/int-reader:path=/tmp/int_socket) OUT -> IN console(console)
