#!/usr/bin/env sol-fbp-runner

# Reads a byte from socket and prints to console.
# See byte-writer.fbp, that should be executed before this.

socket_byte(unix-socket/byte-reader:path=/tmp/byte_socket) OUT -> IN console(console)
