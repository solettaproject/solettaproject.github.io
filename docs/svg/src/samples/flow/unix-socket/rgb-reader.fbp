#!/usr/bin/env sol-fbp-runner

# Reads a sol_rgb from socket and prints to console.
# See rgb-writer.fbp, that should be executed before this.

socket_rgb(unix-socket/rgb-reader:path=/tmp/rgb_socket) OUT -> IN console(console)
