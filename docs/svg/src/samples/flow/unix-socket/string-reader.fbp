#!/usr/bin/env sol-fbp-runner

# Reads a string (sizeof(size_t) with length and then length amount of
# bytes) from socket and prints to console.
# See string-writer.fbp, that should be executed before this.

socket_string(unix-socket/string-reader:path=/tmp/string_socket) OUT -> IN console(console)
